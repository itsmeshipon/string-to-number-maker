function() {
  var s = {{DLV – purchaseValue}};       // your string DL var
  var n = parseFloat(s);                // convert to float
  return isNaN(n) ? undefined : n;      // undefined if invalid
}
